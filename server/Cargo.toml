[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"] }
common = { path = "../common" }
bincode = "2.0.1"
serde = { version = "1.0", features = ["derive"] }